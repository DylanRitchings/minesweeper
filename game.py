class Game:
    def __init__(self, id):
        self.id = id
        # self.numOfPlayers = 3
        # self.currentPlayer = 1
        # self.tilesX = 10
        # self.tilesY = 10
        # self.numOfFlags = 25
        # self.flagsFound = 0
        # self.winningScore = int(self.numOfFlags/2) + 1
        # self.flags = []  # flag positions
        # self.board = [[0 for j in range(self.tilesY)] for i in
        #         range(self.tilesX)]  # creating 2d array to match grid, to store board in
        # self.boardState = [[0 for k in range(self.tilesY)] for n in
        #         range(self.tilesX)]  # creating 2d array to match grid
        # self.playerScores = {}  # dictionary
        # self.scoresText = []  # stores the score strings
